# xcodegen spec — run `xcodegen generate` in this directory on macOS.
# Do not commit the generated .xcodeproj.
name: AnkiIOS
options:
  bundleIdPrefix: com.example
  deploymentTarget:
    iOS: "17.0"
packages:
  SwiftProtobuf:
    url: https://github.com/apple/swift-protobuf
    from: 1.28.0
targets:
  AnkiIOS:
    type: application
    platform: iOS
    sources:
      - Sources
    resources:
      - path: Resources/web
        optional: true
    dependencies:
      - package: SwiftProtobuf
      - framework: ../out/AnkiBridge.xcframework
        embed: false
    settings:
      base:
        SWIFT_OBJC_BRIDGING_HEADER: ../bridge/include/anki_bridge.h
        # Rust staticlibs need these system libs on iOS
        OTHER_LDFLAGS: "-lc++ -framework Security -framework SystemConfiguration"
        # Let Xcode generate Info.plist automatically (required for archive/device builds)
        GENERATE_INFOPLIST_FILE: YES
        PRODUCT_BUNDLE_IDENTIFIER: com.example.ankiios
        MARKETING_VERSION: "0.1.0"
        CURRENT_PROJECT_VERSION: "1"
        INFOPLIST_KEY_UILaunchScreen_Generation: YES
  AnkiIOSTests:
    type: bundle.unit-test
    platform: iOS
    sources:
      - Tests
    dependencies:
      - target: AnkiIOS
